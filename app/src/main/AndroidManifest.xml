<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizApp">

        <!-- FlashActivity as the Launcher Activity -->
        <activity android:name=".FlashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (no intent filter, so it’s not the launcher) -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- QuizActivity (no intent filter) -->
        <activity android:name=".QuizActivity" />

        <!-- Other activities if needed... -->

    </application>
</manifest>
