<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizApp">

        <!-- FlashActivity as the Launcher Activity -->
        <activity android:name=".FlashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (primary entry after FlashActivity) -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- LoginActivity for user authentication -->
        <activity android:name=".LoginActivity" android:exported="true" />

        <!-- SignupActivity for new user registration -->
        <activity android:name=".SignupActivity" android:exported="true" />

        <!-- QuizActivity for the quiz functionality -->
        <activity android:name=".QuizActivity" android:exported="true" />

        <!-- ScoreSummaryActivity to display quiz results -->
        <activity android:name=".ScoreSummaryActivity" android:exported="true" />

        <!-- HistoryActivity for viewing user’s quiz history -->
        <activity android:name=".HistoryActivity" android:exported="true" />

    </application>
</manifest>
